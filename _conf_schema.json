{
  "sidecar_base_url": {
    "description": "XYZW Node sidecar 服务地址",
    "hint": "AstrBot 容器需要能访问到该地址；默认值仅适用于 sidecar 与 AstrBot 在同一网络命名空间运行的场景。",
    "type": "string",
    "default": "http://127.0.0.1:8099"
  },
  "request_timeout": {
    "description": "调用 sidecar 的超时时间（秒）",
    "type": "int",
    "default": 15,
    "min": 3,
    "max": 120
  },
  "notify_mode": {
    "description": "通知策略",
    "hint": "群广播会优先发送到已绑定通知群；仅私聊模式下，通知与测试消息都会直接发给当前用户。",
    "type": "string",
    "options": [
      "group_broadcast",
      "private_only"
    ],
    "default": "group_broadcast"
  },
  "allow_private_fallback": {
    "description": "群广播失败时是否降级为私聊",
    "type": "bool",
    "default": true
  },
  "binding_private_only": {
    "description": "账号绑定等敏感操作是否仅允许私聊",
    "type": "bool",
    "default": true
  },
  "scheduler_poll_interval_seconds": {
    "description": "后台巡检循环的轮询间隔（秒）",
    "type": "int",
    "default": 60,
    "min": 30,
    "max": 600
  },
  "car_reminder_default_interval_minutes": {
    "description": "收车提醒的默认检查间隔（分钟）",
    "type": "int",
    "default": 15,
    "min": 5,
    "max": 720
  },
  "hangup_reminder_default_interval_minutes": {
    "description": "挂机提醒的默认检查间隔（分钟）",
    "type": "int",
    "default": 15,
    "min": 5,
    "max": 720
  },
  "helper_member_reminder_default_interval_minutes": {
    "description": "护卫成员提醒的默认检查间隔（分钟）",
    "type": "int",
    "default": 30,
    "min": 5,
    "max": 720
  },
  "car_helper_query_timeout_ms": {
    "description": "护卫成员查询时调用 sidecar 的超时（毫秒）",
    "type": "int",
    "default": 30000,
    "min": 5000,
    "max": 120000
  }
}
